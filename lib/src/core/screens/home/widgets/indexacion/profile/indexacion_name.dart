import 'package:flutter/material.dart';

class IndexacionName extends StatelessWidget {
  const IndexacionName({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      'Outfits con IA',
      style: TextStyle(
        fontWeight: FontWeight.bold,
        fontSize: 25,
        color: Colors.white,
      ),
    );
  }
}
